# Agents Directory

This compatibility directory exists for tools that look for `.agents/`.

The source of truth for role prompts is `.codex/agents/`. Do not duplicate role
instructions here; add or update files under `.codex/agents/` instead.
